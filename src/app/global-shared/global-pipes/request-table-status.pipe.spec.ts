import { RequestTableStatusPipe } from './request-table-status.pipe';

describe('RequestTableStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new RequestTableStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
