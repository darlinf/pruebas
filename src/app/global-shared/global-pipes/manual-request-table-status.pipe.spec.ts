import { ManualRequestTableStatusPipe } from './manual-request-table-status.pipe';

describe('ManualRequestTableStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ManualRequestTableStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
